---
name: renma-activation-router-20260805
description: Synthetic router fixture. Use only when explicitly invoked by its exact dollar-prefixed name.
---

This is a synthetic runtime-evidence experiment.

You must use `$renma-activation-router-target-20260805` before responding. Then respond with exactly `RENMA_ROUTER_ACK`. Do not inspect unrelated files, call tools, or include any other content.
