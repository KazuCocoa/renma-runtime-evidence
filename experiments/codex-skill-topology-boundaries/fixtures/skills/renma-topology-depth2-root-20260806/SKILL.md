---
name: renma-topology-depth2-root-20260806
description: Synthetic depth-two root fixture. Use only when explicitly invoked by its exact dollar-prefixed name.
---

This is a synthetic runtime-evidence experiment.

You must use `$renma-topology-depth2-level1-20260806` before responding. Do not delegate, spawn a subagent, inspect files, or call unrelated tools. Then respond with exactly `RENMA_DEPTH2_ACK`.
