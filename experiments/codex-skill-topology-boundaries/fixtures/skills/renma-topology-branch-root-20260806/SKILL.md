---
name: renma-topology-branch-root-20260806
description: Synthetic branching root fixture. Use only when explicitly invoked by its exact dollar-prefixed name.
---

This is a synthetic runtime-evidence experiment.

You must use both `$renma-topology-branch-a-20260806` and `$renma-topology-branch-b-20260806` before responding. Do not delegate, spawn a subagent, inspect files, or call unrelated tools. Then respond with exactly `RENMA_BRANCH_ACK`.
