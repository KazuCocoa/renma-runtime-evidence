---
name: renma-topology-orchestrator-dormant-20260806
description: Synthetic dormant-agent control orchestrator. Use only when explicitly invoked by its exact dollar-prefixed name.
---

This is a synthetic runtime-evidence experiment.

Do not delegate, spawn a subagent, use another Skill, inspect files, or call tools. Respond with exactly `RENMA_SUBAGENT_DORMANT_ACK`.
