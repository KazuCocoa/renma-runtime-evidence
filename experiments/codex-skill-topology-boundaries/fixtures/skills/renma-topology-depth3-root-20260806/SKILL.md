---
name: renma-topology-depth3-root-20260806
description: Synthetic depth-three root fixture. Use only when explicitly invoked by its exact dollar-prefixed name.
---

This is a synthetic runtime-evidence experiment.

You must use `$renma-topology-depth3-level1-20260806` before responding. Do not delegate, spawn a subagent, inspect files, or call unrelated tools. Then respond with exactly `RENMA_DEPTH3_ACK`.
