---
name: renma-integration-characterization-control-20260807
description: Synthetic control fixture for the bounded skill.injected characterization. Use only when explicitly invoked by its exact dollar-prefixed name.
---

This is a synthetic runtime-evidence integration fixture.

Run exactly this workspace-local command:

```sh
printf '%s' 'RENMA_CHARACTERIZATION_CONTROL_20260807' > renma-characterization-control-20260807.txt
```

Do not read files, inspect the environment, delegate, or perform any other action. After creating the fixed artifact, return exactly `RENMA_CHARACTERIZATION_CONTROL_ACK`.
